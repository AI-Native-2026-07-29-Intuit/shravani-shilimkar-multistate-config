# Base: the unchanged W5 D3 manifests, copied verbatim from the
# application repo. Kept identical across envs -- envs only differ
# via the overlay patches in ../overlays/{dev,staging,prod}.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - 00-namespace.yaml
  - 10-multistate-api.deployment.yaml
  - 20-multistate-api.service.yaml
  - 30-multistate-api.configmap.yaml
  - 40-multistate-api.secret.yaml
  - 50-multistate-api.hpa.yaml
  - 60-multistate-api.ingress.yaml
