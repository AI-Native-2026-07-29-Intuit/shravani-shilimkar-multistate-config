# TEST: deliberate on-sync-failed drill (Task 4). ResourceQuota is
# explicitly blacklisted at the project level (see
# argocd/projects/multistate.yaml's namespaceResourceBlacklist) --
# this file should be rejected outright, not just fail health.
# Revert immediately after verifying the notification fires.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: multistate-dev-test-quota
  namespace: multistate-dev
spec:
  hard:
    pods: "1"
